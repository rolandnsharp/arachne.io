index thoughts

<ul id="reviews">
  <% @thoughts.each do |thought| %>
    <li>
      
      <span class="user"> <%= thought.content %></span>
      <%= link_to "Delete", user_thought_path(@user, thought) %>
      
      
    </li>
  <% end %>
</ul>